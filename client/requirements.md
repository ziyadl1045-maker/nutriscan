## Packages
html5-qrcode | Barcode scanning functionality
framer-motion | Smooth animations and transitions
clsx | Class name utility
tailwind-merge | Class name utility

## Notes
Authentication uses Replit Auth (redirect to /api/login)
AI Chat endpoints provided by server/replit_integrations/chat
Product lookup endpoint /api/products/:barcode expects external API integration on backend
